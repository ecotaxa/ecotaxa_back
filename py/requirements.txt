# To ensure all is installed and only them:
# (venv)$ pip freeze | xargs pip uninstall -y
# (venv)$ pip install -r requirements.txt

# Typings there depend on SQLA 2.0 interface
SQLAlchemy[mypy]==2.0.51
# sqlalchemy stubs are installed with [mypy] above
psycopg[binary]==3.3.4
# pgvector for similarity search
pgvector==0.4.2
# mac: brew install libjpeg
Pillow==12.2.0
# astral needs pytz
astral==3.2
pytz==2026.2
types-pytz==2026.2.0.20260518
# For image processing
numpy==2.5.0
# Backbone of the app
fastapi==0.138.1
#fastapi-utils==0.2.1
# Pydantic comes with fastapi (2.13.4)
# Speed up openapi response serialization
orjson==3.11.9
types-orjson==3.6.2
#graphene==2.1.8
uvloop==0.22.1
httptools==0.8.0
# Web framework
uvicorn==0.49.0
gunicorn==26.0.0
httpx==0.28.1

lxml==6.1.1
lxml-stubs==0.5.1

requests==2.34.2
types-requests==2.33.0.20260518

python-multipart==0.0.32
# Big files import lib
tuspyserver==4.2.5
# mime type for import
magic-rs==0.3.0
# Auth validation
itsdangerous==2.2.0
passlib==1.7.4
types-passlib==1.7.7.20260211
# A bit of html
jinja2==3.1.6
aiofiles==25.1.0
# Analyzis of XLSX data sources
openpyxl==3.1.5
openpyxl-stubs==0.1.25
# DB upgrade generator
alembic==1.18.5
# CLI tools, for "cmds" subdirectory
typer[all]==0.26.7
# 1.19.1 is the last version which is compatible with the custom plugin
# in API_models/mypy/plugin.py
mypy==1.19.1
# OpenId lib, latest 1.6.6 Requires-Python >=3.9
authlib==1.7.2
types-Authlib==1.6.11.20260518
