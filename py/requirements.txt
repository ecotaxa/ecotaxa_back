# To ensure all is installed and only them:
# (venv)$ pip freeze | xargs pip uninstall -y
# (venv)$ pip install -r requirements.txt

# Typings there depend on SQLA 2.0 interface
SQLAlchemy[mypy]==2.0.51
# sqlalchemy2-stubs are installed with [mypy] above
psycopg[binary]==3.1.19
# pgvector for similarity search
pgvector==0.2.4
# mac: brew install libjpeg
Pillow==10.4.0
# astral needs pytz
astral==2.2
pytz==2021.1
types-pytz==2021.1.2
# For image processing
numpy~=1.26.4
# Backbone of the app
fastapi==0.111.1
#fastapi-utils==0.2.1
# Last (April 2023) pydantic
#pydantic[mypy]==1.10.7
# Speed up openapi response serialization
orjson==3.11.9
types-orjson==3.6.2
#graphene==2.1.8
uvloop==0.22.1
httptools==0.8.0
# Web framework
uvicorn==0.48.0
gunicorn==23.0.0
httpx==0.28.1

lxml==6.1.1
lxml-stubs==0.5.1

requests==2.34.2
types-requests==2.33.0.20260518

python-multipart==0.0.27
# Big files import lib
tuspyserver==4.2.5
# mime type for import
magic-rs==0.2.0
# Auth validation
itsdangerous==2.0.1
passlib==1.7.4
types-passlib==1.7.4
# A bit of html
jinja2==3.1.3
aiofiles==0.7.0
# Analyzis of XLSX data sources
openpyxl==3.0.9
openpyxl-stubs==0.1.21
# DB upgrade generator
alembic==1.13.2
# CLI tools, for "cmds" subdirectory
typer[all]==0.12.3
# The last version which is compatible with the custom plugin in API_models/mypy/plugin.py
mypy==1.19.1
# OpenId lib, latest 1.6.6 Requires-Python >=3.9
authlib==1.3.2
types-Authlib==1.3.0.20241229